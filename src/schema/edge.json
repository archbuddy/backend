{
  "$id": "edge",
  "allOf": [
    {
      "$ref": "diagramItem"
    },
    {
      "type": "object",
      "required": ["name"],
      "properties": {
        "sourceHandle": {
          "type": "string"
        },
        "targetHandle": {
          "type": "string"
        },
        "relation": {
          "$ref": "relation"
        }
      }
    }
  ]
}
